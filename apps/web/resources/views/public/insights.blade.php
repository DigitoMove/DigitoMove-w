@extends('layouts.public') @section('title','Insights')
@section('content')<section class="page-hero section-shell"><span class="kicker">Insights</span><h1>Useful thinking for teams making <em>digital decisions.</em></h1><p>Field notes on software, design, learning, and building for real-world constraints.</p></section><section class="section-shell insights-grid">@forelse($posts as $post)<article class="reveal">@if($post->cover_image)<a href="{{ route('posts.show',$post) }}"><img src="{{ asset($post->cover_image) }}" alt="{{ $post->title }}" class="article-cover"></a>@endif<span class="kicker">{{ $post->category }} · {{ $post->reading_minutes }} min</span><h2><a href="{{ route('posts.show',$post) }}">{{ $post->title }}</a></h2><p>{{ $post->excerpt }}</p><a href="{{ route('posts.show',$post) }}" class="text-link">Read article</a></article>@empty<article><span class="kicker">Editorial</span><h2>The library is ready for its first article.</h2><p>Articles published from the admin will appear here.</p></article>@endforelse</section><div class="section-shell">{{ $posts->links() }}</div>@endsection
