@extends('layouts.public') @section('title','Learning portal')
@section('content')<section class="page-hero section-shell"><span class="kicker">Digito Move Learning</span><h1>Practical technology skills for your <em>next move.</em></h1><p>Structured courses, tutorials, and lessons for beginners, professionals, and ambitious builders.</p></section><section class="section-shell learning-grid">@forelse($courses as $course)<article class="course-card reveal">@if($course->cover_image)<img src="{{ asset($course->cover_image) }}" alt="{{ $course->title }}">@endif<div><span class="kicker">{{ ucfirst($course->level) }} · {{ $course->lessons_count }} lessons</span><h2>{{ $course->title }}</h2><p>{{ $course->summary }}</p><div class="course-meta"><span>{{ $course->duration ?: 'Flexible' }}</span><span>{{ str_replace('_',' ',ucfirst($course->delivery_mode)) }}</span></div><a href="{{ route('courses.show',$course) }}" class="button primary">Explore course</a></div></article>@empty<div class="empty-learning"><h2>Course packages are being prepared.</h2><p>Basic computer training and advanced programming will appear here.</p></div>@endforelse</section>@endsection
