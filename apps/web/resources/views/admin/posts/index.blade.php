@extends('layouts/contentNavbarLayout') @section('title','Articles')
@section('content')<div class="admin-page reveal"><div class="page-heading"><div><span class="eyebrow">Editorial</span><h1>Articles</h1><p>Draft, review, and publish useful thinking.</p></div><a href="{{ route('admin.posts.create') }}" class="btn btn-primary"><i class="bx bx-plus me-1"></i>New article</a></div>@include('content.shared.flash')<div class="card admin-card"><div class="table-responsive"><table class="table admin-table align-middle"><thead><tr><th>Article</th><th>Category</th><th>Status</th><th>Author</th><th>Updated</th><th></th></tr></thead><tbody>@forelse($posts as $post)<tr><td><strong>{{ $post->title }}</strong><small>{{ $post->reading_minutes }} min read</small></td><td>{{ $post->category }}</td><td><span class="status-dot status-{{ $post->status }}">{{ ucfirst($post->status) }}</span></td><td>{{ $post->author?->name ?: '—' }}</td><td>{{ $post->updated_at->diffForHumans() }}</td><td class="text-end"><a href="{{ route('admin.posts.edit',$post) }}" class="btn btn-sm btn-outline-primary">Edit</a></td></tr>@empty<tr><td colspan="6" class="empty-state">Write the first article.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $posts->links() }}</div></div></div>@endsection
