@extends('layouts/contentNavbarLayout') @section('title','Users')
@section('content')<div class="admin-page reveal"><div class="page-heading"><div><span class="eyebrow">Administration</span><h1>Users</h1><p>Manage access, roles, and team details.</p></div><a href="{{ route('admin.users.create') }}" class="btn btn-primary"><i class="bx bx-user-plus me-1"></i>Add user</a></div>@include('content.shared.flash')<div class="card admin-card"><div class="table-responsive"><table class="table admin-table align-middle"><thead><tr><th>User</th><th>Role</th><th>Phone</th><th>Joined</th><th></th></tr></thead><tbody>@foreach($users as $user)<tr><td><strong>{{ $user->name }}</strong><small>{{ $user->email }}</small></td><td><span class="soft-badge">{{ ucfirst($user->role) }}</span></td><td>{{ $user->phone ?: '—' }}</td><td>{{ $user->created_at->format('M j, Y') }}</td><td class="text-end"><a href="{{ route('admin.users.edit',$user) }}" class="btn btn-sm btn-outline-primary">Edit</a></td></tr>@endforeach</tbody></table></div><div class="card-footer">{{ $users->links() }}</div></div></div>@endsection
