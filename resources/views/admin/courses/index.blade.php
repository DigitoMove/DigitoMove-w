@extends('layouts/contentNavbarLayout') @section('title','Courses & Lessons')
@section('content')<div class="admin-page reveal"><div class="page-heading"><div><span class="eyebrow">Learning portal</span><h1>Courses</h1><p>Build structured tutorial packages and manage enrollment.</p></div><a href="{{ route('admin.courses.create') }}" class="btn btn-primary"><i class="bx bx-plus me-1"></i>New course</a></div>@include('content.shared.flash')<div class="row g-4">@forelse($courses as $course)<div class="col-md-6 col-xl-4"><div class="card admin-card h-100"><div class="card-body"><div class="d-flex justify-content-between mb-3"><span class="soft-badge">{{ ucfirst($course->level) }}</span><span class="status-dot status-{{ $course->status }}">{{ ucfirst($course->status) }}</span></div><h4>{{ $course->title }}</h4><p class="text-muted">{{ $course->summary }}</p><div class="d-flex gap-3 mb-4"><small><strong>{{ $course->lessons_count }}</strong> lessons</small><small><strong>{{ $course->applications_count }}</strong> applications</small></div><a href="{{ route('admin.courses.edit',$course) }}" class="btn btn-outline-primary">Manage course</a></div></div></div>@empty<div class="col-12 empty-state">Create your first course package.</div>@endforelse</div><div class="mt-4">{{ $courses->links() }}</div></div>@endsection
