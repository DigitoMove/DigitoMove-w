@extends('layouts/contentNavbarLayout') @section('title','Works & Demos')
@section('content')<div class="admin-page reveal"><div class="page-heading"><div><span class="eyebrow">Portfolio</span><h1>Works & demos</h1><p>Publish projects with screenshots, live links, and safe demo credentials.</p></div><a href="{{ route('admin.projects.create') }}" class="btn btn-primary"><i class="bx bx-plus me-1"></i>Add work</a></div>@include('content.shared.flash')<div class="row g-4">@forelse($projects as $project)<div class="col-md-6 col-xl-4"><div class="card admin-card h-100">@if($project->cover_image)<img src="{{ asset($project->cover_image) }}" style="height:190px;object-fit:cover" alt="{{ $project->title }}">@endif<div class="card-body"><div class="d-flex justify-content-between mb-3"><span class="soft-badge">{{ str_replace('_',' ',ucfirst($project->type)) }}</span><span class="status-dot status-{{ $project->status }}">{{ ucfirst($project->status) }}</span></div><h4>{{ $project->title }}</h4><p class="text-muted">{{ $project->summary }}</p><div class="d-flex gap-2"><a href="{{ route('admin.projects.edit',$project) }}" class="btn btn-outline-primary">Edit</a>@if($project->status==='published')<a href="{{ route('projects.show',$project) }}" target="_blank" class="btn btn-outline-secondary">Preview</a>@endif</div></div></div></div>@empty<div class="col-12"><div class="empty-state">Add the first portfolio project.</div></div>@endforelse</div><div class="mt-4">{{ $projects->links() }}</div></div>@endsection
