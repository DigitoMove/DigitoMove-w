@extends('layouts/contentNavbarLayout') @section('title','Course Applications')
@section('content')<div class="admin-page reveal"><div class="page-heading"><div><span class="eyebrow">Admissions</span><h1>Applications</h1><p>Review applicants and verify application fees.</p></div></div>@include('content.shared.flash')<div class="card admin-card"><div class="table-responsive"><table class="table admin-table align-middle"><thead><tr><th>Applicant</th><th>Course</th><th>Application</th><th>Payment</th><th>Submitted</th><th></th></tr></thead><tbody>@forelse($applications as $application)<tr><td><strong>{{ $application->name }}</strong><small>{{ $application->email }}</small></td><td>{{ $application->course->title }}</td><td><span class="status-dot status-{{ $application->status }}">{{ ucfirst($application->status) }}</span></td><td><span class="soft-badge">{{ ucfirst($application->payments->first()?->status ?? 'none') }}</span></td><td>{{ $application->created_at->diffForHumans() }}</td><td class="text-end"><a href="{{ route('admin.applications.show',$application) }}" class="btn btn-sm btn-outline-primary">Review</a></td></tr>@empty<tr><td colspan="6" class="empty-state">Applications will appear here.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $applications->links() }}</div></div></div>@endsection
