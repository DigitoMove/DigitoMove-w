@extends('layouts.public') @section('title','Application received')
@section('content')<section class="application-result section-shell"><span class="kicker">Application received</span><h1>Keep this payment reference.</h1><p>Your application for <strong>{{ $application->course->title }}</strong> is now in review.</p><div class="payment-reference"><span>Application fee</span><strong>{{ $payment->currency }} {{ number_format($payment->amount) }}</strong><span>Payment reference</span><strong>{{ $payment->reference }}</strong><span>Status</span><strong>{{ ucfirst($payment->status) }}</strong></div><p>Use this reference when paying the application fee. An administrator will verify the payment and update your application.</p><div class="button-row">@if(data_get($payment->metadata,'checkout_url'))<a href="{{ data_get($payment->metadata,'checkout_url') }}" target="_blank" rel="noopener" class="button primary">Pay securely with PesaPal <i class="bx bx-link-external"></i></a>@endif<a href="{{ route('learning') }}" class="text-link">Back to learning portal</a></div></section>@endsection
